# Protocol Loading - V2 metadata.models capacity (PT-075-R1)
# + Experimental model_capabilities seed (PT-ME-001) on DeepSeek

---
suite: "protocol-loading"
name: "Load V2 DeepSeek manifest with Experimental model_capabilities"
id: "load-022"
description: "Verify deepseek v2 validates metadata.models model_capabilities/modalities (PT-ME-001 Experimental; omit=unknown)"
tags: ["protocol-loading", "deepseek", "v2", "metadata", "pt-me-001"]
setup:
  provider: "deepseek"
  manifest_path: "../../../../v2/providers/deepseek.yaml"
input:
  type: "protocol_loading"
  manifest_path: "../../../../v2/providers/deepseek.yaml"
expected:
  valid: true
  errors: []
  provider_id: "deepseek"
  protocol_version: "2.0"

---
suite: "protocol-loading"
name: "Load V2 OpenAI manifest with typed metadata.models capacity"
id: "load-017"
description: "Verify openai v2 provider validates metadata.models context_window/max_output_tokens"
tags: ["protocol-loading", "openai", "v2", "metadata", "pt-075"]
setup:
  provider: "openai"
  manifest_path: "../../../../v2/providers/openai.yaml"
input:
  type: "protocol_loading"
  manifest_path: "../../../../v2/providers/openai.yaml"
expected:
  valid: true
  errors: []
  provider_id: "openai"
  protocol_version: "2.0"

---
suite: "protocol-loading"
name: "Load V2 Gemini manifest with metadata.models capacity"
id: "load-018"
description: "Verify gemini v2 provider validates metadata.models including preview status values"
tags: ["protocol-loading", "gemini", "v2", "metadata", "pt-075"]
setup:
  provider: "gemini"
  manifest_path: "../../../../v2/providers/gemini.yaml"
input:
  type: "protocol_loading"
  manifest_path: "../../../../v2/providers/gemini.yaml"
expected:
  valid: true
  errors: []
  provider_id: "gemini"
  protocol_version: "2.0"
